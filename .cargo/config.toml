[target.wasm32-unknown-unknown]
rustflags = ['--cfg', 'getrandom_backend="wasm_js"']

# NOTE: do not hardcode a Windows MSVC `linker` here. rustc autodetects the
# correct link.exe from the local VS install; an absolute path is machine-
# specific and breaks every other box (it broke CI — see native-build.yml).
