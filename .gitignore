# Rust build output
/target/

# Backup files from rustfmt/cargo-editors
**/*.rs.bk

# macOS
.DS_Store

# Editor/IDE
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/launch.json
!.vscode/tasks.json
captures/
thumbnails/

# wasm-pack build output
/pkg/
/web/pkg
save.json
deltas.json
plants.bin
world_base.bin
# Runtime artifact: written from the default_seeded() presets on first run and by
# the in-app herbarium editor. Canonical species data lives in code
# (src/world_core/plant_gen/species/*.json), so this local copy isn't tracked.
herbarium.json

# Per-instance state from the multi-instance launcher (tools/launch.ts)
/instances/
